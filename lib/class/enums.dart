// ignore_for_file: constant_identifier_names

enum Term { TermsAndCondition, PrivacyPolicy }

enum UserType { User, Company }

enum Load { Waiting, Complete, Done, Successful, Loading, Cancel }

enum AuthStatus { Login, Logout }

enum HiveKey {
  user,
}
